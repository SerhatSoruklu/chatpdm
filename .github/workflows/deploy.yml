name: Deploy ChatPDM

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

jobs:
  deploy:
    runs-on:
      - self-hosted
      - chatpdm-prod
    steps:
      - name: Deploy on dedicated server
        run: bash /srv/chatpdm/repo/scripts/deploy-production.sh main
